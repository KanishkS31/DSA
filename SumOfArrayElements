#include <iostream>
using namespace std;

int main()
{
    int  sum = 0,i, n;
    int arr[] = {1, 2, 3, 4, 5, 6};
    n = sizeof(arr) / sizeof(arr[0]); 
    for (i = 0; i < n; i++)
    sum = sum+arr[i] ;
    cout<<sum;
    return 0;
}
